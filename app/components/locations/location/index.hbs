<li class="{{this.styleNamespace}} {{if @location.activeOutline 'selected'}}" {{on "click" (fn @panTo @location.id)}}>
  <strong style={{this.cellColor}}>{{@location.name}}</strong>
  <span style={{this.color}}>density: {{@location.density.number}}</span>
</li>
{{yield}}