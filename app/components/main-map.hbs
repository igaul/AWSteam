{{#g-map
  styles=this.mapStyle
  lat=this.lat
  lng=this.lng
  zoom=this.zoom
  disableDefaultUI=true

  onLoad=(action "onLoad")
as |g|}}
  {{#each @locations as |location|}}
    {{#each location.shapes as |shape|}}
      {{g.polygon
        path=shape
        geodesic=true
        strokeColor="red"
        strokeOpacity=0
        strokeWeight=1
        fillColor=location.density.color
        fillOpacity=0.5
       }}
    {{/each}}
  {{/each}}
{{/g-map}}
<Locations @locations={{@locations}} @panTo={{this.panTo}} />
{{yield}}